You are the implementation agent for Video Blueprint Studio M1.

Read GEMINI.md first.

Implement Epic E0 only.

Do not integrate real CV models. Do not implement video generation or identity replacement.

Build the smallest validated path:
video upload -> analysis job -> mock pipeline -> blueprint.json -> schema validation -> bundle export.

Stop when E0 acceptance criteria pass.
