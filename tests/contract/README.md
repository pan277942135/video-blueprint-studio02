# Contract tests

Required E0 tests:
- canonical example passes schema
- broken frame range fails
- broken checksum format fails if constrained by schema
- invalid coordinate space/unit fails where required
- API responses conform to OpenAPI/Pydantic models
