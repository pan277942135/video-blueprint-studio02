# E4.2 — Point Tracks / Sparse Motion Evidence

**Status: PASS / runtime-certified (implementation head `14f23a4a889bba4ac8b3ef02a8951e6b15a0343f`)**

## Scope

E4.2 extracts anonymous image-space visual feature motion inside certified E4.1 person masks. It does **not** infer identity, biometrics, demographics, emotion, or cross-video correspondence.

Pipeline slice:

`PERSON_MASK -> POINT_TRACKS`

Dense flow, camera motion, body-local transforms, surface motion and micro-motion remain out of scope.

## Backend

Use the repository's existing OpenCV runtime only:

- Shi-Tomasi (`goodFeaturesToTrack`) for deterministic feature seeding;
- pyramidal Lucas-Kanade (`calcOpticalFlowPyrLK`) for adjacent-frame motion;
- no model weights, network download, or fallback model;
- points must remain inside the current E4.1 person mask;
- hard shot boundaries reset all feature state;
- lost/missing samples are explicit NaN/false and are never interpolated.

Point IDs are local evidence identifiers scoped to one anonymous character in one source video. Re-used storage slots are disambiguated by a monotonically increasing local `track_id` array.

## Manifest contract

E4.2 is emitted under `extensions.e4_point_tracks` because the canonical Character schema has no image-space point-motion slot and `surface_motion` is explicitly body-local. This avoids mislabelling pixel-space measurements as body-local evidence.

Each character may reference one NPZ sidecar containing:

- `positions_xy`: float32 `[frame, point_slot, 2]` in pixels;
- `valid`: bool `[frame, point_slot]`;
- `tracking_error`: float32 `[frame, point_slot]`, NaN when unavailable;
- `track_id`: int64 `[frame, point_slot]`, `-1` when invalid.

The TimeSeriesRef describes `positions_xy`; metadata names the companion arrays and declares the mask constraint, shot reset, algorithm, and slot semantics. `BlueprintValidator` additionally enforces pixel-space semantics, timeline/shape, SHA256 checksums, local character cross-references, no interpolation, and config-hash consistency.

## Certified implementation evidence

Implementation head `14f23a4a889bba4ac8b3ef02a8951e6b15a0343f` passed all gates:

- Epic E1 CI **#148**, run `31759579750`: PASS — Contract, TypeScript, build, true HTTP E2E, full pytest, Ruff and mypy;
- E4.1 RTMDet-Ins regression **#18**, run `31759579731`: PASS;
- E4.2 Sparse Motion Certification **#7**, run `31759579691`: PASS.

Real-media certification used the already certified E2 detector and E4.1 mask checkpoints with exact existing hashes; E4.2 itself introduced **no new model weights**. It ran the public E4 pipeline on a deterministic translated real-human sample and produced:

- pipeline version: `0.4.0-e4.2`;
- anonymous characters: `5`;
- point-track refs: `5`;
- valid point samples: `3302`;
- adjacent same-feature motion samples: `2862`;
- median motion: `1.117929 px`;
- p95 motion: `1.165209 px`;
- point-track coverage: `1.0`;
- identity inference performed: `false`;
- biometric embedding exported: `false`;
- new model weights introduced: `false`.

Certification artifact:

- artifact id: `9204090521`;
- digest: `sha256:6ca8f5179f1f35ed16df2fb4cada98f692904fe7c83da69ee9e4e8669c9b1910`.

The E4.2 certification workflow includes this plan file in its PR path filter, so evidence-only edits are also re-certified instead of relying on an older code-head green state.

## Static-analysis note

A narrow transitional lint/type exception remains scoped only to the pre-existing public `packages/pipeline_core/e4_media_pipeline.py` compatibility module because GitHub's write-safety layer repeatedly blocked an in-place cleanup of that legacy module during this branch. The actual new sparse-motion implementation remains under normal Ruff/mypy checks. This exception does not disable global lint/type gates and is tracked as technical debt rather than weakening runtime certification.

## Gates

E4.2 is PASS only when all of the following hold:

1. deterministic synthetic translation recovers the known displacement within tolerance;
2. full E4.1-mask -> E4.2 sidecar integration is schema-valid;
3. every emitted URI resolves to a physical artifact and checksum;
4. missing values remain explicit and no temporal interpolation occurs;
5. no point continuity crosses a hard shot boundary;
6. standard CI, Ruff and mypy pass;
7. real-media certification demonstrates non-zero feature motion and validates final Blueprint/Bundle evidence;
8. E4.1 real-runtime regression remains green.

**Technical gate: PASS.** Merging the final PR into `main` remains a project-owner approval gate.
